import React from "react";
import styles from "../style";

const Hero = () => {
  return (
    <div
      className={`border-2 border-indigo-500/100 ${styles.heading2} ${styles.flexCenter} ${styles.padding}`}
    >
      Circles
    </div>
  );
};

export default Hero;
